# 《深度学习》课程实验报告
## 实验5：循环神经网络实验

韩佳轩，23281106，计算机科学与技术学院

---


### 一、实验目标

1. 掌握序列数据处理方法，包括基本处理（固定长度滑动窗口）、高级处理（重采样、插分、填充与打包）以及面向对象的数据集构造。
2. 理解并实现循环神经网络（RNN）、长短期记忆网络（LSTM）和门控循环单元（GRU）的基本原理。
3. 掌握使用PyTorch自带模块（`torch.nn.RNN`、`torch.nn.LSTM`、`torch.nn.GRU`）实现模型的方法。
4. 在至少一种数据集（高速公路车流量数据或用户签到数据）上进行实验，对比分析不同模型（RNN、LSTM、GRU）在预测任务中的表现。
5. 进行超参数对比分析（如`hidden_size`、`batch_size`、`lr`等），并可视化实验结果。

---

### 二、实验内容

#### 2.1 使用数据集

实验过程中，采用PPT中提到的**高速公路车流量**数据集。

参考`数据集说明.ipynb`文件，使用面向对象方法封装`TrafficDataset`类。

**TrafficDataset数据集类**声明：
```python
class TrafficDataset:
    def __init__(self, sensor=49, target=0):
        # 选取适当的检测器用作序列数据
        self.raw_data = np.load('./dataset/traffic-flow/traffic.npz')['data']
        self.sensor = sensor
        self.target = target
        # 数据标准化
        self.min = self.raw_data.min()
        self.max = self.raw_data.max()
        self.data = (self.raw_data - self.min) / (self.max - self.min)

    def denormalize(self, x):
        return x * (self.max - self.min) + self.min

    def construct_set(self, train_por, test_por, window_size, label=0):
        train_x = []
        train_y = []
        val_x = []
        val_y = []
        test_x = []
        test_y = []

        len_train = int(self.data.shape[0] * train_por)
        train_seqs = self.data[:len_train, self.sensor, :]
        for i in range(train_seqs.shape[0] - window_size):
            train_x.append(train_seqs[i:i + window_size])
            train_y.append(train_seqs[i + window_size, self.target])
        
        len_val = int(self.data.shape[0] * (train_por + test_por))
        val_seqs = self.data[len_train:len_val, self.sensor, :]
        for i in range(val_seqs.shape[0] - window_size):
            val_x.append(val_seqs[i:i + window_size])
            val_y.append(val_seqs[i + window_size, self.target])
        
        test_seqs = self.data[len_val:, self.sensor, :]
        for i in range(test_seqs.shape[0] - window_size):
            test_x.append(test_seqs[i:i + window_size])
            test_y.append(test_seqs[i + window_size, self.target])

        train_set = my_Dataset(torch.Tensor(train_x), torch.Tensor(train_y))
        val_set = my_Dataset(torch.Tensor(val_x), torch.Tensor(val_y))
        test_set = my_Dataset(torch.Tensor(test_x), torch.Tensor(test_y))
        return train_set, val_set, test_set
```

在训练过程中，使用设定数据集类设置数据迭代器。
```python
from datasets import TrafficDataset

# 数据加载,使用TrafficDataset
print("Loading dataset...")
dataset = TrafficDataset(sensor=96)
train_set, val_set, test_set = dataset.construct_set(
        train_por=0.6, test_por=0.2, window_size=window_size
    )
    
train_loader = DataLoader(train_set, batch_size=batch_size, shuffle=True)
val_loader   = DataLoader(val_set,   batch_size=batch_size, shuffle=False)
test_loader  = DataLoader(test_set,  batch_size=batch_size, shuffle=False)
    
input_size = train_set.X.shape[-1] 
print(f"Input size: {input_size}, Samples: train={len(train_set)}, val={len(val_set)}, test={len(test_set)}")
```

#### 2.2 循环神经网络（RNN）

1. **基本原理**：处理任意长度的时序数据，隐状态随时间更新。
2. **模型实现**：
   - 手动实现RNN（定义参数、更新隐状态）
   ```python
   class MyRNN(nn.Module):
    def __init__(self, input_size, hidden_size, output_size=1):
        super().__init__()
        self.hidden_size = hidden_size
        
        # 可学习参数
        self.w_h = nn.Parameter(torch.rand(input_size, hidden_size))
        self.u_h = nn.Parameter(torch.rand(hidden_size, hidden_size))
        self.b_h = nn.Parameter(torch.zeros(hidden_size))
        
        self.w_y = nn.Parameter(torch.rand(hidden_size, output_size))
        self.b_y = nn.Parameter(torch.zeros(output_size))
        
        # 激活函数
        self.tanh = nn.Tanh()
        self.leaky_relu = nn.LeakyReLU()
        
        # 参数初始化（Xavier）
        for param in self.parameters():
            if param.dim() > 1:
                nn.init.xavier_uniform_(param)
    
    def forward(self, x):
        batch_size = x.size(0)
        seq_len = x.size(1)
        
        h = torch.zeros(batch_size, self.hidden_size).to(x.device)
        y_list = []
        
        for i in range(seq_len):
            h = self.tanh(
                torch.matmul(x[:, i, :], self.w_h) +
                torch.matmul(h, self.u_h) + 
                self.b_h
            )
            y = self.leaky_relu(
                torch.matmul(h, self.w_y) + self.b_y
            )
            y_list.append(y)
        
        return torch.stack(y_list, dim=1), h
    ```
   - 使用`torch.nn.RNN`接口
   ```python
   class TorchRNN(nn.Module):
    def __init__(self, input_size, hidden_size, output_size=1, num_layers=1):
        super().__init__()
        self.rnn = nn.RNN(input_size, hidden_size, num_layers, batch_first=True)
        self.output = nn.Linear(hidden_size, output_size)
    
    def forward(self, x):
        out, _ = self.rnn(x)
        return self.output(out), None  # 返回out和dummy hidden
    ```
3. **训练与评估**：
   - 使用`utils.py`中编写的RMSE、MAE、MAPE作为评估指标
   - 可视化Loss、RMSE、MAE、MAPE变化曲线

#### 2.3 长短期记忆网络（LSTM）

1. **基本原理**：通过细胞状态和门控机制（遗忘门、输入门、输出门）解决长程依赖问题。
2. **模型实现**：
   - 手动实现LSTM（参数初始化、门控计算）
   ```python
   class MyLSTM(nn.Module):
    def __init__(self, input_size, hidden_size, output_size=1):
        super().__init__()
        self.hidden_size = hidden_size
        
        # 四个门共用一个线性层（i, f, o, g）
        self.gates = nn.Linear(input_size + hidden_size, hidden_size * 4)
        
        # 激活函数
        self.sigmoid = nn.Sigmoid()
        self.tanh = nn.Tanh()
        
        # 输出层（可选，这里直接用最后一个隐藏状态预测）
        self.output_layer = nn.Linear(hidden_size, output_size)
        
        # 参数初始化（Xavier）
        for param in self.parameters():
            if param.dim() > 1:
                nn.init.xavier_uniform_(param)
    
    def forward(self, x):
        batch_size = x.size(0)
        seq_len = x.size(1)
        
        # 初始化隐藏状态和细胞状态
        h = torch.zeros(batch_size, self.hidden_size).to(x.device)
        c = torch.zeros(batch_size, self.hidden_size).to(x.device)
        
        y_list = []
        
        for i in range(seq_len):
            # 拼接输入和上一时刻隐藏状态
            combined = torch.cat([x[:, i, :], h], dim=-1)
            
            # 计算四个门
            gates = self.gates(combined)
            input_gate, forget_gate, cell_gate, output_gate = gates.chunk(4, dim=-1)
            
            input_gate = self.sigmoid(input_gate)
            forget_gate = self.sigmoid(forget_gate)
            cell_gate = self.tanh(cell_gate)
            output_gate = self.sigmoid(output_gate)
            
            # 更新细胞状态和隐藏状态
            c = forget_gate * c + input_gate * cell_gate
            h = output_gate * self.tanh(c)
            
            # 输出（这里使用隐藏状态进行预测，可根据需要调整）
            y = self.output_layer(h)
            y_list.append(y)
        
        return torch.stack(y_list, dim=1), (h, c)
    ```
   - 使用`torch.nn.LSTM`接口。
   具体实现过程中，只需更改`torch.nn.RNN`中的相关函数即可。下同。

#### 2.4 门控循环单元（GRU）

1. **基本原理**：简化LSTM门控机制，通过重置门和更新门控制信息流。
2. **模型实现**：
   - 手动实现GRU
   ```python
   class My_GRU(nn.Module):
    def __init__(self, input_size, hidden_size, output_size):
        super().__init__()
        self.hidden_size = hidden_size
        
        self.gates = nn.Linear(input_size+hidden_size, hidden_size*2)
        # 用于计算candidate hidden state
        self.hidden_transform = nn.Linear(input_size+hidden_size, hidden_size)
        
        self.sigmoid = nn.Sigmoid()
        self.tanh = nn.Tanh()
        
        self.output = nn.Sequential(
            nn.Linear(hidden_size, hidden_size // 2),
            nn.ReLU(),
            nn.Linear(hidden_size // 2, output_size)
        )
            
        for param in self.parameters():
            if param.dim() > 1:
                nn.init.xavier_uniform_(param)
                
    def forward(self, x):
        batch_size = x.size(0)
        seq_len = x.size(1)
        
        h = torch.zeros(batch_size, self.hidden_size).to(x.device)
        y_list = []
        for i in range(seq_len):
            update_gate, reset_gate = self.gates(torch.cat([x[:, i, :], h], dim=-1)).chunk(2, -1)
            update_gate, reset_gate = (self.sigmoid(gate) for gate in (update_gate, reset_gate))
            candidate_hidden = self.tanh(self.hidden_transform(torch.cat([x[:, i, :], reset_gate * h], dim=-1)))
            h = (1-update_gate) * h + update_gate * candidate_hidden
            y_list.append(self.output(h))
        return torch.stack(y_list, dim=1), h
    ```
   - 使用`torch.nn.GRU`接口

---

### 三、实验步骤

1. **数据处理**：根据实验要求完成`datasets.py`中缺失的代码，实现数据读取、标准化、滑窗采样、数据封装等。
2. **模型实现**：
   - 分别手动实现和调用接口实现RNN、LSTM、GRU
   - 编写训练循环，保存模型和训练日志
3. **超参数实验**：对比不同`hidden_size`、`batch_size`、`lr`对模型性能的影响。本实验中对`lr`，`hidden_size`进行分析。
4. **结果对比**：在同一数据集上对比RNN、LSTM、GRU的训练时间、预测精度、Loss变化等。同时进行误差的可视化。

---

### 四、实验结果与分析

#### 4.1 模型性能对比

| 模型 | RMSE | MAE | MAPE(%) | 训练时间(s) |
| :---: | :---: | :---: | :---: | :---: |
| MyRNN  | 19.7133 | 14.9891 | 8.38 | 68.83 |
| TorchRNN | 21.1982 |  16.2638 | 9.54 | 23.49 |
| MyLSTM | 19.7264 | 15.181 | 8.36 | 145.69 |
| TorchLSTM | 19.6281 | 15.0753 | 8.22 | 45.24 |
| MyGRU | 19.97 | 15.2783 | 8.77 | 97.03 |
| TorchGRU | 19.4052 | 14.8298 | 8.22 | 35.06 |

#### 4.2 超参数影响分析

实验中，使用之前的TorchRNN实现进行实验。
| 参数变化 | RMSE | MAE | MAPE(%) | 训练时间(s) |
| :---: | :---: | :---: | :---: | :---: |
| 原参数[^1] | 20.0663 | 15.4852 | 8.52 | 19.68 |
| lr=0.0005 | $\downarrow$ 19.6823 | $\downarrow$ 14.9885 | $\downarrow$ 8.39 | $\uparrow$ 32.91 |
| hidden_size=128 | $\uparrow$ 20.6882 | $\uparrow$ 16.1266 | $\uparrow$ 9.70 | $\downarrow$ 14.23 |

通过实验可以看出，降低lr可以提高训练效果，但会增长训练时间。
提高hidden_size相反，降低训练时间的同时可能会略微降低训练效果。

---

### 五、实验总结与体会

通过本次实验，深入理解了RNN、LSTM、GRU的结构与工作原理。掌握了序列数据预处理、模型构建、训练与评估的全流程。通过对比实验，进一步认识到不同模型在时序预测任务中的优缺点。学会了使用PyTorch的数据加载器、模型接口和优化工具，提升了代码实现能力。


[^1]: sensor=96, window_size=12, hidden_size=64, learning_rate=0.001, batch_size=64, num_epochs=50, device=cuda